#!/bin/sh

# Sets and enables SASL system logging
export RELEASE_SYS_CONFIG="$RELEASE_ROOT/releases/$RELEASE_VSN/sys"

# If you run the release in a container, you should set this
# to the external IP where the container can be reached
# export RELEASE_DISTRIBUTION=name
# export RELEASE_NODE=<name>@<external IP>
